import React from "react";

function cap() {
    const caption = <p>Start a new campaign game.</p>
    
    return ( 
        <div className="cap">
        {caption}
        </div>
    );
}

export default cap;